// Centralized configuration
// Leave empty to use relative paths (recommended when serving via Nginx/Traefik with proxying)
// Set to 'http://localhost:8000' for local development if running frontend separately
export const API_BASE_URL = 'http://localhost:8000';
